# Raw game data & extraction output (large binaries, not needed for website)
/_extract
/extracted
/.claude

# Web build artifacts
/web/node_modules
/web/dist

# Image junctions (local dev only – too large for git)
/web/public/items
/web/public/icons

# Python artifacts
/scripts/__pycache__
/scripts/generated_fbs
*.pyc

# IDE / tool local data
/.claude
